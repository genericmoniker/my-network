# My network setup

- Pi-hole ad blocking
- Custom DNS block notifier
- WireGuard VPN
- Inadyn dynamic DNS updater

Start/update:

```
ssh kylo
cd my-network
docker compose up --build --detach --pull always
```
